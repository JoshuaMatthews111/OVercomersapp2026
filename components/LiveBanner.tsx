import React from 'react';

/**
 * The "We're live" card on Home. Placeholder created 2026-09-22 so the events
 * work (which owns Home) and the live-streaming work (which owns this file)
 * could be built at the same time. It renders nothing until the live work
 * fills it in.
 */
export function LiveBanner() {
  return null;
}
